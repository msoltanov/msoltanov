{
  "name": "alabay-code-profile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "generate": "node scripts/generate-profile.mjs",
    "test": "node --test",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "@xmldom/xmldom": "0.9.12"
  }
}
